import 'package:flutter/material.dart';

class PodacastScreen extends StatelessWidget {
  const PodacastScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
